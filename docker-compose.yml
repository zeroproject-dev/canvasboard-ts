services:
  app:
    container_name: canvasboard
    image: canvasboard
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - 80
    networks:
      - proxy

networks:
  proxy:
    external: true
